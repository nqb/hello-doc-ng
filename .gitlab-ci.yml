stages:
 - build

# variable def0
# my_global_variable sets in UI to "UI value"


# variable def1
variables:
  my_global_variable: global value
 
before_script:
  - echo $my_global_variable

# variable def2  
.build_template:
  variables:
    my_global_variable: extends value

build1:
  stage: build
  extends:
    - .build_template
  script:
    - echo $my_global_variable

build2:
  stage: build
# variable def3  
  variables:
    my_global_variable: job value
  script:
    - echo $my_global_variable

build3:
  stage: build
  script:
    - echo $my_global_variable
