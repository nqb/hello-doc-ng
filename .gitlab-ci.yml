stages:
 - build

variables:
  my_global_variable: global value
 
before_script:
  - echo $my_global_variable

.build_template:
  variables:
    my_global_variable: extends value

build1:
  stage: build
  extends:
    - .build_template
  script:
    - echo $my_global_variable

build2:
  stage: build
  variables:
    my_global_variable: job value
  script:
    - echo $my_global_variable

